# OmniSolve 3.0 Requirements
# Core dependencies for the refactored architecture

# HTTP client for API calls (KoboldCPP backend)
requests>=2.31.0

# Async file I/O
aiofiles>=23.2.1

# Optional: Progress bars (uncomment if desired)
# tqdm>=4.66.0

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Type checking stubs
types-requests>=2.31.0

# Note: Python 3.8+ required for type hints and async features

# Optional system monitoring
psutil>=5.9.0

# --- Optional LLM Backends ---
# OpenAI / OpenAI-compatible APIs (e.g. LM Studio, Together AI):
#   pip install openai>=1.0.0
# Anthropic Claude API:
#   pip install anthropic>=0.20.0
# Or install both with:
#   pip install omnisolve-ai-system[all]
